# Dockerfile

Defines how an image is built.

Important Instructions:
- FROM
- WORKDIR
- COPY
- RUN
- ENV
- EXPOSE
- ENTRYPOINT
- CMD

Best Practice:
Package the application first, then copy only the artifact into the runtime image.
