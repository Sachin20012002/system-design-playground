# Docker Networks

Purpose:
Allow containers to communicate securely.

Concepts:
- bridge
- host
- none
- overlay

Docker provides an internal DNS.
Containers communicate using service/container names instead of IP addresses.

Remember:
localhost inside a container refers to that container itself.
